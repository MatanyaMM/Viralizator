# Copy this file to .env and fill in your values
# (The app reads these from the Settings page in the dashboard,
#  but you can also pre-populate the DB via this file if you add a seed script)

PORT=3000

# Optional: Redis URL for BullMQ job queues
# Leave blank to use in-memory processing (dev mode)
REDIS_URL=

# API Keys — set these via the dashboard Settings page at runtime
# APIFY_TOKEN=
# OPENAI_API_KEY=
# GEMINI_API_KEY=
# PUBLIC_BASE_URL=https://your-domain.com
